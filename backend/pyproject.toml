[project]
name = "real-estate-ai-backend"
version = "0.1.0"
description = "FastAPI backend for Real Estate AI scraping (CrewAI + Bright Data MCP)"
requires-python = ">=3.9,<3.13"
dependencies = [
    "crewai-tools[mcp]>=0.17.0",
    "crewai>=0.86.0",
    "mcp>=1.0.0",
    "python-dotenv>=1.0.0",
    "pandas>=2.0.0",
    "uvicorn[standard]>=0.30.0",
    "fastapi>=0.115.0",
    "litellm>=1.80.0",
]

[tool.uv]
# Optional: use uv for installs
